import { createFileRoute, Link } from "@tanstack/react-router";
import { BarChart3, Bell, FileText, GitCompareArrows, UserRound } from "lucide-react";

import { CaseMindShell } from "@/components/CaseMindShell";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/dashboard")({ head: () => ({ meta: [{ title: "User Dashboard — CaseMind" }] }), component: DashboardPage });
function DashboardPage() { const widgets = [{ icon: FileText, title: "Saqlangan vaziyatlar", value: "3 demo" }, { icon: BarChart3, title: "Oldingi tahlillar", value: "12 report" }, { icon: GitCompareArrows, title: "Taqqoslash natijalari", value: "5 compare" }, { icon: UserRound, title: "Profil", value: "Faol" }, { icon: Bell, title: "Notification system", value: "4 xabar" }]; return <CaseMindShell><section className="mx-auto max-w-7xl px-5 py-16 sm:px-8"><p className="text-sm font-semibold uppercase text-accent">User Dashboard</p><h1 className="mt-3 text-5xl font-black text-foreground sm:text-6xl">Shaxsiy huquqiy markaz</h1><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{widgets.map((item) => <article key={item.title} className="rounded-2xl border bg-card p-6 shadow-premium"><item.icon className="mb-5 h-8 w-8 text-accent" /><h2 className="text-2xl font-black text-foreground">{item.title}</h2><p className="mt-3 text-muted-foreground">{item.value}</p></article>)}<article className="rounded-2xl border bg-card p-6 shadow-premium"><h2 className="text-2xl font-black text-foreground">Premium obuna</h2><p className="mt-3 text-muted-foreground">PDF report, case compare va priority konsultatsiya.</p><Button className="mt-5" variant="premium" asChild><Link to="/pricing">Premium olish</Link></Button></article></div></section></CaseMindShell>; }
